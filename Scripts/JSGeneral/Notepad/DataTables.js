  //------------------------change settings on fte fly-----------------------------------------------------------------------------------------------------------------------------
 oTable = $("#my-table").dataTable(oST);
 var oSettings = oTable.fnSettings();
 oSettings.sAjaxSource  = "new value";
 //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
oTable.fnAdjustColumnSizing();
oTable.fnDraw();
 //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
var aData=oTable.fnGetData(oTable.fnGetPosition($("tr.row_selected")[0])); 
$(_fnRowCallback(nTr, oTable.fnGetData(oTable.fnGetPosition($("tr.row_selected")[0]))).html()
 //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//Delete row
 var row = $(this).closest("tr").get(0);
oTable.fnDeleteRow(oTable.fnGetPosition(row));

	$('td', oTable.fnGetNodes()).click( function () {
		oTable.fnDeleteRow(this.parentNode,null,true);
	} );

	
